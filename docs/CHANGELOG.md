# Changelog

## Sprint 042 — Unified Strategy-Intent Evaluation & Portfolio Decision Bridge

Added the deterministic **Unified Strategy-Intent Evaluation & Portfolio Decision Bridge** (`services/market-engine/src/intelligence/strategy-intent-evaluation/`, 23 modules + fixtures, 17 test files) closing the loop **StrategyIntent → Source Validation → Intent Integrity → Evidence → Safety → Comparability → Freshness → Stability → Dependency → Restriction Analysis → Portfolio-Interface Compatibility → Evaluation Dimensions → Classification → Downstream Eligibility → Research/Feedback Escalation → Explanation → Boundary → Audit → Invariants → Replay** over ACTUAL validated Sprint 041 intent outputs (consumed read-only). **THE ENGINE IS AN ANALYTICAL DECISION BRIDGE, NOT A PORTFOLIO ENGINE, NOT A RISK ENGINE, NOT AN ALLOCATOR, NOT A STRATEGY REGISTRY, NOT AN EXECUTION PLANNER AND NOT AN ORACLE.** It answers "Is this StrategyIntent sufficiently supported, constrained, comparable, fresh, stable and structurally valid to be considered by the existing Portfolio/Risk/Allocation decision plane?" — it produces an analytical eligibility package only; it never allocates capital, computes authoritative weights, reserves capital, modifies positions, creates orders or execution plans, calls Treasury or AEGIS, accesses provider credentials or exchange/sportsbook APIs, or mutates Portfolio, Risk, Allocation, Strategy Registry, OIIN, Research or Learning state; eligibility NEVER means approved for trading, betting, execution, capital allocation or strategy activation — the downstream plane decides. Delivered: fail-closed source validation consuming only governed Sprint 041 results (frozen results, verified `oship.strategy-intent.v1` audit chains with replay-seal tolerance, bound audit identities, invariant/replay flags, complete provenance with pinned versions, and a recomputed whole-result fingerprint — a result mutated after sealing rejects with INVALID_INTENT_SOURCE); a structural intent-integrity gate (classification ↔ evidence/freshness/comparability agreement, informational baseline present, unique known restriction codes, classification-cause restrictions, NORMALIZED_COMPARISON_ONLY ↔ COMPARABLE_VIA_NORMALIZATION agreement, dependency state ↔ flag agreement, actionable intents must carry historical support and a preferred alternative); eight lifecycle gates; ten semantic-safety vocabularies with clause-scoped negation and quoted-span stripping over annotations and narratives (boundary-continuation phrases like "Aegis stays the execution authorization author" are inert); exactly 18 explicit evaluation dimensions (no hidden scoring, no ML); 13 deterministic classifications (EVALUATION_ALLOWED, _ALLOWED_WITH_LIMITATIONS, _REQUIRES_RESEARCH, _BLOCKED, _INSUFFICIENT_EVIDENCE, _NOT_COMPARABLE, _CONFLICTED, _STALE, _UNSTABLE, _STRATEGY_DEPENDENT, _VENUE_DEPENDENT, _REGIME_DEPENDENT, _MIXED) with fixed precedence — blocked sources never un-blocked, actionable+UNSTABLE → UNSTABLE, RESEARCH_REQUIRED+MULTI → MIXED, single-flag dependencies → their family state, deficient gates demote actionable intents (the harder state wins); the frozen classification → eligibility map onto 9 downstream states (ELIGIBLE_FOR_CONSIDERATION, ELIGIBLE_WITH_RESTRICTIONS, RESEARCH_REQUIRED, BLOCKED, NOT_COMPARABLE, INSUFFICIENT_EVIDENCE, STALE, UNSTABLE, CONFLICTED) — blocked families surface no alternatives, dependency families require research first; AFIS BUY/SELL and ABL BACK/LAY preserved verbatim (never collapsed, never converted, ABL market/selection identity + decimal odds > 1, AFIS no betting fields) and one intent never spans both domains; raw cross-domain NOT_COMPARABLE with explicit-declaration normalization; Sprint 041 restrictions carried verbatim — never weakened — plus the derived DOWNSTREAM_CONSIDERATION_ONLY boundary code in canonical order; Sprint 041 research requirements carried with explicit provenance plus evaluation-derived classes (MIXED escalates regime+strategy+venue research); eight feedback kinds to the existing Learning/Feedback architecture; deterministic explanations pinning source ids with declared semantic limitations; provenance Opportunity → Decision → Governance → Strategy Intent → Evaluation with every engine version pinned; the nine protected downstream authorities (Portfolio, Risk, Allocation, Strategy Registry, AEGIS, Treasury, Execution, Research Plane, Learning/Feedback) with forbidden-key and execution-verb scans and the four-check bridge boundary; canonical key-order-independent byte-identical serialization, content-derived timestamp-free prefixed ids (eval_/evctx_/evdim_/evres_/evrsc_/evfdb_/evprv_/evexp_/evbnd_/evea_/evfp_/evcfg_), internal double-run byte-compare over result and audit chain, and replay-from-serialized with tamper detection; a hash-chained `oship.strategy-intent-evaluation.v1` audit over 19 event types where tampering, reordering, substitution, truncation, extension, foreign events, provenance/classification/eligibility mismatch all fail verification; **86 hard fail-closed invariants** per run; and a 70-entry rejection gallery exercising all 29 distinct codes (INVALID_EVALUATION_CONTEXT … AUDIT_INTEGRITY_FAILURE). On the real corpus: clean AFIS → EVALUATION_ALLOWED (2 alternatives surfaced), strengthened clean ABL surebet → EVALUATION_ALLOWED (4 alternatives, BACK/LAY preserved), liquidity-imbalance → EVALUATION_ALLOWED_WITH_LIMITATIONS (8 restrictions, 2 research classes), aging → ALLOWED_WITH_LIMITATIONS, normalized → ALLOWED_WITH_LIMITATIONS (NORMALIZED_COMPARISON_ONLY carried), AFIS CVA → EVALUATION_CONFLICTED, ABL thin surebet → EVALUATION_INSUFFICIENT_EVIDENCE, stale/unknown-freshness → EVALUATION_STALE, unstable → EVALUATION_UNSTABLE, governance-blocked/authority-bypass/unstable-blocked → EVALUATION_BLOCKED, not-comparable → EVALUATION_NOT_COMPARABLE, venue/strategy/regime single-flag → EVALUATION_{VENUE,STRATEGY,REGIME}_DEPENDENT (RESEARCH_REQUIRED), venue-dependent multi-flag → EVALUATION_MIXED, and the governance ANY_DEPENDENCY escalation → EVALUATION_REQUIRES_RESEARCH — all thirteen classifications and all nine eligibility states live. Covered by 764 new Sprint 042 tests across 17 files (full suite 6264/6264) and `npm run demo:strategy-intent-evaluation` (80 assertion-backed sections ending `SYSTEM STATUS: RECONCILED`). Deterministic only: no ML, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Portfolio/Risk/Allocation/Strategy/Treasury/Execution/Research/Learning authority, no second AEGIS.


## Sprint 041 — Unified Strategy Decision Synthesis & AEGIS-Ready Intent Engine

Added the deterministic **Unified Strategy Decision Synthesis & AEGIS-Ready Intent Engine** (`services/market-engine/src/intelligence/strategy-intent/`, 24 modules + fixtures, 31 test files) closing the loop **Governed Decision + Governance Result → Source Validation → Intent Context → Governance/Decision Facts → Classification → Priority → Objective → Alternatives → Ranking → Dependencies → Restrictions → Research → Evidence → Explanation → Feedback → Boundary → Audit → Invariants → Replay** over ACTUAL validated Sprint 040 governance outputs (consumed read-only). **THE ENGINE IS AN INTENT SYNTHESIS LAYER, NOT A STRATEGY ENGINE, NOT AN EXECUTION ENGINE, NOT A CAPITAL ALLOCATOR AND NOT AN ORACLE.** It answers "Given a governed Decision Intelligence handoff, what structured strategic intent should the existing Strategy authority receive?" — it produces a strategy-INPUT intent, never a strategy, never an execution order; it sits strictly between Decision Governance and the existing Strategy layer (Decision Intelligence → Decision Governance → Strategy Intent → Existing Strategy → AEGIS → Treasury → Execution), never creating a parallel Strategy authority and never mutating the Strategy Registry. Delivered: fail-closed source validation consuming only governed Sprint 040 outputs (frozen results, verified audit chains, bound audit-identity heads, invariant/replay flags, provenance pairing — mismatched pairs are orphans and reject with INVALID_PROVENANCE); an immutable intent context mirroring governance and decision identity verbatim; eight intent classifications (STRATEGIC_INTENT_READY, _READY_WITH_LIMITATIONS, _RESEARCH_REQUIRED, _BLOCKED, _NOT_COMPARABLE, _CONFLICTED, _STALE, _INSUFFICIENT_EVIDENCE) mapped 1:1 from the eight handoff classifications with NO silent fallback — blocked families surface `preferredAlternativeId: null` and an empty acceptable set, and a classification must be supported by its own evidence gates (an allowed handoff carrying conflicted/insufficient/not-comparable evidence rejects with CONFLICTED_EVIDENCE / INSUFFICIENT_EVIDENCE / NOT_COMPARABLE); six analytical priority levels (NORMAL_STRATEGY_INPUT, LIMITED_STRATEGY_INPUT, HIGH_RESEARCH_PRIORITY, RESEARCH_ONLY, CRITICAL_GOVERNANCE_REVIEW, BLOCKED) that order governance attention and research — never probability, return or urgency; six analytical objective classes (PRESERVE_EVIDENCE_SUPPORTED_EDGE, PREFER_STABLE_ALTERNATIVE, PREFER_HISTORICALLY_SUPPORTED_ALTERNATIVE, MINIMIZE_EVIDENCE_CONFLICT, REQUIRE_MORE_RESEARCH, NO_ACTIONABLE_INTENT); alternative sets preserving canonical ids, semantic identity, evidence state, compatibility and limitations with roles PREFERRED/SECONDARY/REJECTED/UNSUPPORTED and deterministic ordering (blocked intents gate the acceptable list behind actionability); AFIS legs carry BUY/SELL with financial identity and no betting fields, ABL legs carry BACK/LAY with market/selection identity and decimal odds > 1 — never collapsed, never converted (BACK never becomes BUY); raw AFIS↔ABL stays NOT_COMPARABLE while normalized comparison requires the explicit, versioned, governance-attested, loss-declared normalization — and the NORMALIZED_COMPARISON_ONLY restriction must agree with the COMPARABLE_VIA_NORMALIZATION status or the input rejects; six dependency states preserved from governance (never removed because a preferred alternative exists); seventeen intent restriction codes (the four-intent informational baseline ANALYTICAL_ONLY/NO_EXECUTION/NO_TREASURY_ACTION/NO_AEGIS_AUTHORIZATION on EVERY intent, plus governance-mapped and intent-derived codes) surviving serialization and replay; eight research escalation classes routed to the existing Research Plane (governance escalations carried verbatim with provenance plus intent-derived EVIDENCE_REFRESH, STABILITY_RESEARCH, ALTERNATIVE_RESEARCH, COMPARABILITY_RESEARCH); eight feedback kinds routed to the existing Learning/Feedback architecture (no new authorities) — including ALTERNATIVE_PRESERVED_FEEDBACK for actionable intents that surface secondaries without a preferred; evidence-backed rationale with explicitly historical support ("never a future claim") and semantic limitations declaring normalization loss; deterministic explanations pinning source decision and governance ids; provenance Opportunity → Opportunity Intelligence → Decision → Governance → Strategy Intent with no orphan intents and every upstream engine version pinned; the verbatim disclaimer "This is an evidence-bound strategic intent, not a probability, forecast, expected return, guarantee, or execution instruction."; a strategy boundary where the intent is an input and `strategyDecides: true` — no order quantity/price, venue command, API request, credential, signing material, Treasury transfer, capital allocation, AEGIS authorization or bookmaker/exchange submission is possible, all fail closed (quoted requester speech inside governance reasons is inert reported text); content-derived prefixed ids (`sint_`/`sctx_`/`sprv_`/`sobj_`/`sres_`/`srsc_`/`srcx_`/`sfdb_`/`sexp_`/`sbnd_`/`sea_`/`sfp2_`) with zero timestamp, random, PID, machine or insertion-order dependence; canonical key-order-independent byte-identical serialization and replay (internal double-run, permuted-key replay, replay-from-serialized with tamper detection); a hash-chained `oship.strategy-intent.v1` audit over 16 event types where tampering, reordering, substitution, truncation, extension and foreign events fail verification; **93 hard fail-closed invariants** per run; and a 57-entry rejection gallery exercising 24 distinct codes over the 25-code vocabulary (INVALID_INTENT_CONTEXT … AUDIT_INTEGRITY_FAILURE). On the real corpus: AFIS CVA → STRATEGIC_INTENT_CONFLICTED (MINIMIZE_EVIDENCE_CONFLICT, CRITICAL_GOVERNANCE_REVIEW, 10 restrictions, no preferred), ABL surebet → STRATEGIC_INTENT_INSUFFICIENT_EVIDENCE (RESEARCH_ONLY, 7 restrictions, all alternatives UNSUPPORTED), liquidity-imbalance → STRATEGIC_INTENT_READY_WITH_LIMITATIONS (PREFER_STABLE_ALTERNATIVE, LIMITED_STRATEGY_INPUT, prefers alt-venue-a, 7 restrictions), venue-dependent → STRATEGIC_INTENT_RESEARCH_REQUIRED (HIGH_RESEARCH_PRIORITY), clean → STRATEGIC_INTENT_READY (NORMAL_STRATEGY_INPUT, baseline restrictions only), plus no-dominant (secondaries preserved without a preferred), stale/aging/unknown-freshness/unstable/not-comparable/normalized/governance-blocked and authority-bypass runs. Covered by 560 new Sprint 041 tests across 31 files (full suite 5500/5500) and `npm run demo:strategy-intent` (60 assertion-backed sections ending `SYSTEM STATUS: RECONCILED`). Deterministic only: no ML, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Strategy/Research/Learning/Treasury/Risk/Portfolio/Execution authority, no second AEGIS.

## Sprint 040 — Unified Decision Governance & Strategy Handoff Engine

Added the deterministic **Unified Decision Governance & Strategy Handoff Engine** (`services/market-engine/src/intelligence/governance/`, 23 modules + public surface + fixtures, 24 test files) closing the loop **Decision Result → Governance Context → Policy Validation → Evidence Gate → Safety Gate → Comparability Gate → Freshness Gate → Stability Gate → Dependency Gate → Handoff Classification → Strategy Handoff Package → Research / Feedback** over ACTUAL validated Sprint 039 decision-intelligence results (consumed read-only). **THE ENGINE IS A GOVERNANCE AND HANDOFF LAYER, NOT A STRATEGY ENGINE, NOT AN EXECUTION ENGINE, NOT A CAPITAL ALLOCATOR AND NOT AN ORACLE.** It answers "Is this decision result sufficiently valid, comparable, fresh, stable and policy-compliant to hand to Strategy, and what information and limitations must accompany the handoff?" Every output is informational-only; no probabilities, forecasts, expected returns, ROI, guaranteed outcomes or execution instructions are ever produced; it never bypasses or duplicates Strategy Registry, AEGIS, Treasury, Execution, Portfolio, Risk or Allocation and sits strictly inside Intelligence, upstream of Strategy. Delivered: an immutable governance context mirroring Sprint 039 states verbatim with fail-closed upstream validation (invariant/replay violations, NaN/Infinity payloads via recursive scan, fabricated output keys, AFIS/ABL semantic violations — BUY/SELL on ABL legs → INVALID_BACK_LAY_SEMANTICS, odds on AFIS legs → INVALID_AFIS_SEMANTICS, odds ≤ 1 or missing market/selection identity → INVALID_ABL_SEMANTICS); twelve explicit deterministic policies (evidence sufficiency, stale evidence, conflicted evidence, comparability, stability, leakage, regime/strategy/venue dependency, research gaps, semantic safety, authority boundaries) each validated, versioned and audited per run — rejections never hidden behind generic "not recommended"; the six gates with explicit states (evidence PASS/PASS_WITH_LIMITATIONS/BLOCK_INSUFFICIENT_EVIDENCE/BLOCK_CONFLICTED/BLOCK_STALE/BLOCK_NOT_COMPARABLE under precedence NOT_COMPARABLE → CONFLICTED → INSUFFICIENT → STALE; safety SAFE/BLOCK_UNSAFE with negation-aware certainty scans — "support is not certainty" stays legal; comparability with BUY/SELL and BACK/LAY semantically distinct and raw cross-domain comparison structurally impossible — normalized comparison only under an explicit `cross-domain.normalized.v1` total mapping onto a neutral namespace with declared semantic loss, version match and policy attestation, never inferred, and domain-spanning alternative sets rejected before any gate runs; freshness FRESH/AGING/STALE/UNKNOWN worst-of where STALE blocks unless `allowStaleAnalyticalOnly` and UNKNOWN is never silently FRESH; stability STABLE/MODERATELY_STABLE/UNSTABLE/INSUFFICIENT never converted to probabilities with optional `unstableBlocksHandoff`; dependency INDEPENDENT/REGIME_/STRATEGY_/VENUE_/MULTI_DEPENDENT/UNKNOWN preserved into the package); an authority check over seven protected authorities where bypass attempts become auditable in-band blocks; eight handoff classifications (ALLOWED, ALLOWED_WITH_LIMITATIONS, REQUIRES_RESEARCH, BLOCKED, NOT_COMPARABLE, CONFLICTED, STALE, INSUFFICIENT_EVIDENCE) under deterministic precedence where blocked handoffs surface `recommendedAlternativeId: null` and are never silently converted into alternatives; thirteen machine-readable restriction codes with canonical ordering and scopes (every handoff carries ANALYTICAL_ONLY/NO_EXECUTION/LIMITED_TO_DOMAIN); deterministic immutable handoff packages (`decision-governance.handoff.v1`) with the full evidence-bound framing (opportunity identity, domain, recommendation, alternative ranking, evidence summary, trade-offs, dominance, evidence limitations, dependencies, leakage/stability/freshness/comparability status, research requirements, governance result and restrictions, source versions, verbatim disclaimer, audit identity) plus a minimal informational strategy-input view (`decision-governance.strategy-input.v1`, `strategyDecides: true`) — containing no executable orders, execution instructions, Treasury/capital-allocation commands, AEGIS authorization, provider credentials or raw exchange/bookmaker credentials; seven research escalation kinds routed to the existing Research Plane and structured feedback records (blocked, allowed-with-limitations, evidence gap, stale, conflict, dependency detected, recommendation weakened/preserved) routed to the existing Learning/Feedback architecture — no new authorities; byte-identical replay (internal double-run byte-compared, key-order independent, annotation-order independent); a hash-chained `oship.decision-governance.v1` audit over exactly 16 event types where tampering, reordering, substitution, truncation, extension and foreign events fail verification; 77 hard fail-closed invariants; content-derived prefixed ids (`gov_`/`gctx_`/`gevg_`/`gsfg_`/`gcmp_`/`gfsh_`/`gstb_`/`gdep_`/`gaut_`/`gcls_`/`gres_`/`grsc_`/`grcx_`/`gfdb_`/`ghof_`/`gstr_`/`gea_`) with no timestamp, randomness, PID or machine-identity dependence; and a 22-entry rejection gallery over explicit codes. On the real corpus: AFIS CVA → HANDOFF_CONFLICTED (MULTI_DEPENDENT, 7 restrictions), ABL surebet → HANDOFF_INSUFFICIENT_EVIDENCE (INDEPENDENT, 4 restrictions), liquidity-imbalance → HANDOFF_ALLOWED_WITH_LIMITATIONS (5 restrictions, recommends alt-venue-a, leakage investigation escalated), venue-dependent run → HANDOFF_REQUIRES_RESEARCH (8 restrictions), clean run → HANDOFF_ALLOWED (all twelve policies PASS, baseline restrictions only), plus stale/aging/unknown-freshness/unstable/not-comparable/normalized/authority-bypass/unsafe-annotation runs. AFIS (BUY/SELL, no odds, no betting identity) and ABL (BACK/LAY, decimal odds > 1, market+selection identity) stay isolated end to end; BACK is never BUY and LAY is never SELL. Covered by 610 new Sprint 040 tests across 24 files (full suite 4940/4940) and `npm run demo:decision-governance` (54 assertion-backed sections ending `SYSTEM STATUS: RECONCILED`). Deterministic only: no ML, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Research/Learning/Treasury/Risk/Portfolio/Execution authority, no second AEGIS.

## Sprint 039 — Unified Decision Intelligence & Counterfactual Evaluation Engine

Added the deterministic **Unified Decision Intelligence & Counterfactual Evaluation Engine** (`services/market-engine/src/intelligence/decision/`, 24 modules + fixtures, 35 test files) closing the loop **Opportunity → Opportunity Intelligence → Decision Context → Candidate Alternatives → Compatibility → Counterfactual Evaluation → Trade-off Analysis → Dominance → Evidence-Bound Recommendation → Explanation → Research / Strategy Input** over the ACTUAL validated Sprint 037 learning result and Sprint 038 opportunity profiles (consumed read-only). **THE ENGINE IS AN ANALYTICAL DECISION-SUPPORT LAYER, NOT AN EXECUTION ENGINE, NOT A CAPITAL ALLOCATOR AND NOT AN ORACLE.** Counterfactual means "What does the existing evidence say about this alternative if this alternative had been selected?" — never "What will happen in the future?" Every result carries `informational: true` and `ASSOCIATIONAL_ONLY`; no probabilities, forecasts, expected returns, ROI, guaranteed outcomes or execution instructions are ever produced; the engine sits strictly upstream of Strategy and AEGIS and never mutates Strategy Registry/Portfolio/Risk/AEGIS/Treasury/Execution or provider credentials. Delivered: fail-closed alternative validation over twelve rejection codes with a twelve-entry rejection gallery; an auto-constructed BASELINE alternative per decision (explicit nulls = no override) and per-domain standard alternative builders (AFIS strategy/venue/execution; ABL orientation/market/venue — never cross-domain); compatibility gating over ten-plus checks (domain, class, identity, semantics, unit, ordering) before any evaluation; deterministic counterfactual candidates via explicit override application with venue-leg merging; per-pair descriptive-only historical comparison; six dependency axes (regime, strategy, venue, leakage, stability, evidence) where dependencies are surfaced, never silently generalized (venue-specific evidence is never aggregated away); the twelve-dimension explicit trade-off score (historicalPreservation, realizationQuality, evidenceQuality, similarityQuality, regimeFit, strategyFit, venueFit, leakageBurden, stabilityPenalty, evidenceGaps penalty, complexity, freshness) with config-driven auditable weights, availability renormalization, bitwise-exact decomposition and null scores on INSUFFICIENT/NOT_COMPARABLE/CONFLICTED confidence; dominance analysis over nine states where DOMINANT_BY_EVIDENCE means "better supported by existing evidence" and explicitly never certainty; evidence-bound recommendations with exact disclaimer and null selection under conflict or insufficient evidence; scenario matrices with observation-backed cells only and explicit INCOMPATIBLE markers for unsupported combinations; reconstructible explanations (no opaque scores, no certainty claims, no authority language); informational research contexts with prioritized questions; decision feedback records and immutable outcome divergence reconciliation (AGREEMENT / RECOMMENDED_BUT_NEGATIVE / SPURNED_ALTERNATIVE_POSITIVE / INSUFFICIENT_RECOMMENDATION / UNDETERMINABLE plus regime/strategy/venue/leakage mismatch flags — drift observations, never history rewrites); byte-identical replay (the engine double-runs internally and byte-compares); a hash-chained `oship.decision-intelligence.v1` audit over 14 event types (tamper/reorder/substitution/truncation/extension all fail closed); and 83 hard fail-closed invariants. On the real corpus: AFIS CVA five-alternative decision CONFLICTED (aggressive strategy carries the leakage share; conflicts never forced to a winner), ABL surebet four-alternative decision INSUFFICIENT_EVIDENCE (cohorts n≤2 → null scores, all excluded with reasons), liquidity-imbalance venue comparison DOMINANT_BY_EVIDENCE under tight margins (0.6063 vs 0.6593, margin 0.053), venue-dependent run VENUE_DEPENDENT (dependency band 0.08), plus regime-dependent (band 0.10), no-dominant, baseline-only, back-lay orientation-swap and cross-domain-rejection runs. AFIS (BUY/SELL, no odds, no betting identity) and ABL (BACK/LAY, decimal odds > 1, market+selection identity) stay isolated end to end; orientation swaps preserve BACK/LAY meaning exactly; raw cross-domain comparison is structurally impossible. Covered by 530 new Sprint 039 tests across 35 files (full suite 4330/4330) and `npm run demo:decision-intelligence` (48 assertion-backed sections ending `SYSTEM STATUS: RECONCILED`). Deterministic only: no ML, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Treasury/Risk/Portfolio/Execution authority, no second AEGIS.

## Sprint 038 — Unified Predictive Opportunity Intelligence & Evidence-Bound Scoring Engine

Added the deterministic **Unified Predictive Opportunity Intelligence & Evidence-Bound Scoring Engine** (`services/market-engine/src/intelligence/opportunity/`, 22 modules + tests) closing the loop **New Opportunity → Historical Similarity → Learned Features → Regime Match → Strategy History → Venue History → Leakage History → Evidence Quality → Stability → Historical Outcome Distribution → Opportunity Intelligence Profile → Evidence-Bound Score → Classification → Explanation → Research / Decision Input** over the ACTUAL validated Sprint 037 learning result (consumed read-only; its invariants and replay must have passed). **THE ENGINE IS AN ANALYTICAL EVIDENCE LAYER, NOT AN AUTHORITY AND NOT AN ORACLE.** Every profile carries `informational: true` and `ASSOCIATIONAL_ONLY`; it never claims guaranteed profit, execution, future probability, expected return, financial certainty or betting certainty; it never mutates Strategy Registry/Portfolio/Risk/AEGIS/Treasury/Execution or provider credentials. Delivered: fail-closed candidate validation over thirteen rejection codes with a fifteen-entry rejection gallery; deterministic five-component explicit-feature similarity (same-domain pool only, floor 0.6, top-K 20, canonical tie-breaks); domain-scoped learned-feature summaries with honest INSUFFICIENT for absent subjects; nearest-era regime matching by explicit proximity (UNAVAILABLE when nothing measurable); classification→fit maps for strategy and venue histories; leakage accounting with the burden counted exactly once (adjusted = realized + leakage); evidence profiles with source/count/adequacy/freshness/consistency/completeness/conflicts/comparability and explicit confidence where heterogeneous-but-uncontradicted is WEAK (scored, penalized) and missing evidence is never negative evidence (it blocks scoring); Sprint 037 stability integration (explicit factors, never silent overrides); descriptive-only historical outcome distributions under the exact distribution disclaimer; the eleven-dimension evidence-bound score with config-driven auditable weights, availability renormalization, bitwise-exact decomposition, null scores on INSUFFICIENT/NOT_COMPARABLE/CONFLICTED/STALE confidence and the exact disclaimer "This is an evidence-bound analytical index, not a probability, forecast, expected return, or guarantee."; dependency detection (regime/strategy/venue cohort spread rule with all groups exposed); the deterministic classification precedence with reconstructible reasons; per-domain deterministic ranking with explicit exclusions; reconstructible explanations with no opaque scores, no certainty claims and no authority language; structured informational research contexts with gaps and recommended questions; decision feedback records and immutable outcome reconciliation; byte-identical replay (the engine double-runs internally); a hash-chained `oship.opportunity-intelligence.v1` audit over 13 event types (tamper/reorder/substitution/truncation/extension all fail closed); and 66 hard fail-closed invariants. On the real corpus: 7 candidates assessed (AFIS cross-venue-arbitrage guardian STRATEGY_DEPENDENT score 0.766, market-making HISTORICALLY_FAVORABLE 0.749, liquidity-imbalance MIXED 0.599, aggressive CONFLICTED → null, stale STALE → null; ABL surebet/back-lay INSUFFICIENT n≤2 → null) and 2 rejected in-band. AFIS (BUY/SELL, no odds, no betting identity) and ABL (BACK/LAY, decimal odds > 1, market+selection identity) stay isolated end to end; raw cross-domain comparison is structurally impossible. Covered by 488 new Sprint 038 tests across 29 groups (full suite 3800/3800) and `npm run demo:opportunity-intelligence` (48 assertion-backed sections ending `SYSTEM STATUS: RECONCILED`). Deterministic only: no ML, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Treasury/Risk/Portfolio/Execution authority, no second AEGIS.

## Sprint 037 — Unified Intelligence Learning & Feedback Engine

Added the deterministic **Unified Intelligence Learning & Feedback Engine** (`services/market-engine/src/intelligence/learning/`, 30 modules) closing the loop **Findings → Learning Signals → Opportunity/Strategy Intelligence → Future Research Priorities → Intelligence Feedback** over the ACTUAL Sprint 036 research result. **THE LEARNING PLANE IS NOT AN AUTHORITY. PAPER ONLY.** Every learning output carries `informational: true`; it never authorizes trades, bets, capital, risk, policy or execution; policy candidates are flagged, never promoted (`OUTSIDE_ENGINE`); Sprint 036 findings are consumed read-only and never overwritten. Deterministic by construction: identical inputs (order-independent) yield byte-identical observations, cohorts, learnings, signals, priorities, feedback, audit chains and fingerprints — all aggregations run over canonical observation order so even floating-point sums are permutation-invariant. Fail-closed with explicit states: INSUFFICIENT_EVIDENCE drifts carry null deltas, empty cohorts are never comparable, contradicted strategies surface as CONTRADICTORY, confidence is null when not honestly computable, and no fabricated probability/expected-return keys exist anywhere. Causal safety is ASSOCIATIONAL_ONLY by default with forbidden-language (caused/guarantees/will produce/proves/ensures) rejected at build time. One shared engine serves AFIS (BUY/SELL legs) and ABL (BACK/LAY legs) with domain-correct semantics; raw cross-domain economics are NOT_COMPARABLE — only the explicit DOMAIN_NORMALIZED path may average domains. Delivered: 65 observations (5 eras, AFIS+ABL), 65 features, per-subject feature vectors, 35 cohorts across 9 dimensions, explicit baselines, 5 learnings (3 strategies, 10 classes, 2 venues, 3 policy groups, 34 leakage components), 5 regimes across 6 dimensions, 23 drift assessments, 20 stability assessments, 8 confidence assessments, 67 signals across 8 kinds, 55 research priorities (informational), 11 recommendations, 12 feedback items (3 new research queries, 8 evidence gaps, 1 priority update); byte-identical replay including input-order permutations; a hash-chained `oship.intelligence-learning.v1` audit over 19 event types (tamper/reorder/substitution/truncation/extension all fail closed); and 47 hard fail-closed invariants including the seven no-authority-mutation checks. Covered by 477 new Sprint 037 tests across 33 groups and `pnpm demo:intelligence-learning` (44 assertion-backed sections ending `SYSTEM STATUS: RECONCILED`). Deterministic only: no ML, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Treasury/Risk/Portfolio/Execution authority, no second AEGIS.

## Sprint 036 — Historical Intelligence & Research Plane

Added the deterministic **Unified Historical Intelligence, Research & Learning Plane** (`services/market-engine/src/intelligence/research/`, 27 modules) — an immutable, queryable analytical memory over ACTUAL Sprint 035 closed-loop results: Historical Records → Normalization → Intelligence Memory → Knowledge Graph → Research Queries → Pattern Detection → Comparative Analysis → Hypotheses → Evidence Evaluation → Findings → Rankings → Intelligence Feedback (+ Recommendations), closed by Replay, a hash-chained audit and hard invariants. **THE RESEARCH PLANE IS NOT AN AUTHORITY. PAPER ONLY.** It recommends, ranks, classifies and reports; it never mutates Treasury, Portfolio, Risk, AEGIS, Execution, the Strategy Registry or any active policy; every feedback item carries `informational: true` and ranking is never authorization. History is immutable: corrections create NEW record versions with lineage, provenance and reason — never in-place edits. Deterministic by construction: identical inputs (order-independent) yield byte-identical memory, graph, results, findings, rankings and fingerprints. Fail-closed with explicit epistemic states (UNKNOWN / UNAVAILABLE / INSUFFICIENT_EVIDENCE / CONTRADICTORY / NOT_COMPARABLE / REJECTED) — nothing is ever fabricated, unavailable values never contribute, and correlation is never promoted to fact (hypotheses stay hypotheses with statuses SUPPORTED / WEAKLY_SUPPORTED / CONTRADICTED / INSUFFICIENT_EVIDENCE / REJECTED). One shared engine serves AFIS and ABL with BACK/LAY semantics preserved; cross-domain comparisons are rejected unless explicitly normalized. Comparability always comes first: minimum samples per side, identical metric definitions and compatible capital scale/provenance/class overlap — incomparable populations are NOT_COMPARABLE with explicit reasons and are never silently ranked. Pattern detection covers all six families (preservation improvement/deterioration/consistently-high/low, leakage recurrence across all Sprint 035 components, strategy divergence/outperformance/high-theoretical-poor-realization, venue leakage/fill degradation/adverse drift, policy stability/regression/improvement-without-end-to-end, failure recurrence) — every pattern carries evidence counts and confidence. Delivered: 65-observation five-era memory (11 index dimensions), 192-node/2353-edge knowledge graph (13 node types, 15 relations), 9 named analytical queries, 6 canonical comparisons, 77 patterns, 8 hypotheses across the full epistemic spectrum, 9 findings, 7 rankings, 8 feedback kinds, 10 research recommendations; byte-identical replay; a hash-chained `oship.historical-research.v1` audit over 14 event types (tamper/reorder/substitution/truncation all fail closed, `replay-completed` terminator enforced); and 46 hard fail-closed invariants. Covered by 350 new tests across 27 groups and `pnpm demo:historical-research` (35 assertion-backed sections ending `SYSTEM STATUS: RECONCILED`). Deterministic only: no ML, no random search, no natural-language query surface, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Treasury/Risk/Portfolio/Execution authority, no second AEGIS.

## Sprint 035 — Market-to-Execution Closed-Loop Intelligence & Attribution

Added the deterministic **Unified Market-to-Execution Closed-Loop Intelligence & Attribution Engine** (`services/market-engine/src/intelligence/closed-loop/`, 32 modules) closing the outer loop from discovery to realized value: OIIN → Opportunity → Strategy → Allocation → Risk → Execution Plan → Execution Control → Execution Performance → Attribution → Realized Opportunity Value → Closed-Loop Intelligence. One engine serves AFIS and ABL with deterministic versioned classification and BACK/LAY semantics preserved. Answers with explicit provenance and fail-closed guarantees: opportunity existence, theoretical edge at Risk-approved capital scale, strategy selection, allocation, risk constraints (PROTECTIVE_CONSTRAINT vs rejection vs loss vs movement vs uncertainty — Risk never scored by realized profit), the plan, adaptations (improving and degrading), costs, edge survival (preservation ratio), a 17-component leakage decomposition summing exactly to theoreticalNet − realizedNet, realized net value, and which strategies/venues/policies preserve the most value. Comparable analysis groups never silently mix incomparable opportunities; the analytical ranking is intelligence, never authorization; recommendations are informational only. Byte-identical replay; hash-chained `oship.closed-loop-intelligence.v1` audit over exactly 15 event types (tamper/reorder/substitution/truncation fail closed); 35 hard invariants. NOT AN AUTHORITY: no mutation of Treasury/Portfolio/Risk/AEGIS/Execution, no Strategy Registry or active-policy modification, no live APIs, no provider credentials, single shared Treasury model preserved. Covered by 419 new tests across 32 groups and `pnpm demo:closed-loop-intelligence` (37 assertion-backed sections ending `SYSTEM STATUS: RECONCILED`).

## Sprint 034 — Execution Performance Intelligence & Policy Optimization

Added the deterministic, paper-only **Unified Execution Performance Intelligence & Policy Optimization Engine**. `services/market-engine/src/execution/performance/` closes the canonical loop **Control Sessions → Observations → Attribution → Benchmarks → Quality → Venue/Strategy/Domain Intelligence → Policy Evaluation → Deterministic Parameter Optimization → Policy Candidate → Simulation Gate → Regression Gate → Promotion Gate → auditable candidate**. **PERFORMANCE INTELLIGENCE AND POLICY OPTIMIZATION ARE NOT AUTHORITIES. PAPER ONLY.** The layer recommends, never deploys: the active policy is never mutated, candidates may be promoted only through explicit approval (ELIGIBLE ≠ ACTIVE), and Risk/AEGIS/Treasury/Portfolio/Execution remain the canonical authorities. Delivered: immutable sourced observations (one per cycle×venue, `SourcedValue` provenance, fail-closed telemetry validation — invalid or contradictory telemetry throws, unavailable metrics are marked UNAVAILABLE and never fabricated); 12-component cost attribution (FEES/SPREAD/SLIPPAGE/MARKET_IMPACT/LATENCY/ADVERSE_SELECTION/PARTIAL_FILL/REROUTE/REPRICE/RESLICE/REPLAN/FAILURE_RECOVERY) reconciled against measured outcomes within tolerance; six benchmark kinds (arrival/decision/VWAP/best-observed-venue/simulated-reference/policy-baseline) with explicit MEASURED/SIMULATED/DERIVED/UNAVAILABLE provenance that is never mixed; a 9-dimension deterministic execution-performance quality score with A–F grades; venue scorecards with sample-sufficiency isolation, six statuses and no overfitting; strategy-level scores without changing the Strategy Registry; one engine for AFIS and ABL (domain adapters only); policy evaluation with deterministic scores and sample sufficiency; a bounded gridded parameter space (11 descriptors over thresholds, hysteresis bands, action budgets and quality thresholds) with six PROTECTED safety paths that can never be optimized and validation rejecting unknown/off-grid/out-of-bounds entries; deterministic optimization only (COORDINATE + bounded-exhaustive GRID, no random search, no ML) optimizing the versioned canonical objective Quality−Cost−Slippage−Impact−Latency−Failure−Adaptation−Incompletion; immutable versioned policy candidates (v1→v1.1→v1.2→v2 lineage) that never modify the active policy; a simulation gate re-running the real Sprint 033 control engine on identical deterministic inputs and producing baseline + candidate results and 9 deltas; a regression gate enforcing 12 protected conditions (quantity reconciliation, execution correctness, fail-closed behavior, risk/AEGIS boundaries, emergency-stop preservation, budget limits, deterministic replay, lineage/audit integrity, AFIS/ABL semantics) so optimization may never trade safety for performance; a deterministic promotion state machine (INSUFFICIENT_DATA/REJECTED/SIMULATION_FAILED/REGRESSION_FAILED/IMPROVEMENT_INSUFFICIENT/ELIGIBLE/APPROVED_CANDIDATE) requiring both relative and absolute improvement; immutable policy lineage with exact replay; byte-identical analysis replay (metrics, ranking, candidate, fingerprint, audit chain); a hash-chained `oship.execution-performance.v1` audit log over 12 event types; and 27 hard fail-closed invariants. Covered by **378 new tests across 27 groups** and `pnpm demo:execution-performance` (36 assertion-backed sections — including adverse execution, venue degradation, partial fills, repeated adaptive actions, a candidate that improves quality but violates a protected constraint → REJECTED, and a genuinely better candidate → ELIGIBLE — ending `SYSTEM STATUS: RECONCILED`). Deterministic only: no ML, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Treasury/Risk/Portfolio/Execution authority, no second AEGIS.

## Sprint 033 — Autonomous Execution Control Plane

Added the deterministic, paper-only **Autonomous Execution Control Plane** — one control engine over the Sprint 030–032 execution stack. `services/market-engine/src/execution/control/` closes each control cycle as **Execution Plan → Simulation → Telemetry → Execution Intelligence → Control Cycle → Decision → Safety/Risk/AEGIS validation → Action → Checkpoint → Feedback → next cycle → Completion/Abort/Exhaustion**. **THE CONTROL PLANE IS NOT AN AUTHORITY. PAPER ONLY.** It observes, evaluates, requests validation/authorization, submits execution revisions and receives results; it never mutates Treasury or Portfolio, overrides Risk, bypasses AEGIS, executes directly or calls provider APIs — the authority bridge exposes exactly risk + aegis + execution with literal `treasuryMutation/portfolioMutation: false` markers enforced by invariant. Delivered: a 12-state control machine (INITIALIZED/OBSERVING/EVALUATING/DECIDING/VALIDATING/EXECUTING/WAITING_FEEDBACK/REASSESSING/REPLANNING/COMPLETED/ABORTED/EXHAUSTED) with explicit validated audited transitions and no skipped semantically-required states; immutable fingerprinted control cycles (cycleId/executionPlanId/parentCycleId/cycleNumber/startedAt/completedAt/state/telemetry/signals/quality/decision/action/result/configurationFingerprint/inputFingerprint/outputFingerprint); per-action budgets (maxCycles/maxReprices/maxReslices/maxReroutes/maxReplans/maxFailures/maxExecutionTimeMs) and hard limits (maxSlippageBps/maxImpactNotional/maxLatencyMs) with current/max/remaining accounting, deterministic rejection reasons and explicit EXHAUSTED termination (never a silent continue); precedence-resolved decisions (EMERGENCY_STOP > HARD_RISK_VIOLATION > AEGIS_REJECTION > ABORT > REPLAN > REROUTE > REPRICE/RESLICE > WAIT > CONTINUE > COMPLETE) where safety is never overridden and any non-APPROVED authority status (REJECTED **or PENDING**) fails closed; multi-cycle feedback (current vs previous vs baseline: improving/degrading/oscillation/repeated failure/rerouting/repricing/diminishing improvement/recovery); deterministic oscillation detection (venue flip-flop A→B→A, repeated identical actions, action ping-pong → ABORT or REPLAN per config); hysteresis for quality bands and venue health with recovery thresholds ≠ degradation thresholds; a completion engine (target filled + atomic legs + risk + AEGIS + reconciliation + no unresolved mandatory actions, partial distinguishable); an abort engine with 12 canonical reasons that preserves history/telemetry/audit/lineage/filled+remaining quantity and records a terminal ABORT revision in lineage (through the Execution authority) before terminating; narrow authority bridges with no duplicate authorities; per-cycle checkpoints carrying full recovery journals (completed cycles, prior checkpoints, next sequence, budgets, lineage, venue/quality/risk/aegis state, applied-action keys, audit events) with verifiable fingerprints; deterministic recovery that refuses tampered/foreign checkpoints, never re-applies an action and is byte-identical to the uninterrupted run; byte-equivalent deterministic replay (two replays byte-identical); a hash-chained tamper-evident `oship.execution-control.v1` audit log (12 lifecycle event types); and 24 hard fail-closed invariants (deterministic transitions/decisions, immutable cycles/plans, lineage chain, quantity preservation at every revision boundary, budget monotonicity/ceilings, no duplicate actions/fills, emergency-stop dominance, fail-closed semantics, authority boundaries, checkpoint/recovery consistency, replay equivalence, atomic integrity, AFIS/ABL domain + semantic-side compatibility, no live execution). One control engine serves AFIS and ABL (BACK→BUY/long, LAY→SELL/short preserved across lineage). Covered by **302 new tests across 23 groups** (state-machine 15, transitions 5, control-cycles 16, budgets 16, limits 9, decision-priority 14, multi-cycle-feedback 14, oscillation 13, hysteresis 10, completion 12, abort 13, authority-bridge 12, checkpoints 14, recovery 12, replay 12, audit 14, invariants 19, afis 15, abl 15, cross-domain 10, determinism 15, fail-closed 15, emergency-stop 12) and `pnpm demo:execution-control` (23 assertion-backed sections: all 8 actions, sequential adaptations, venue failure/recovery, venue + action oscillation, hysteresis, budget exhaustion, emergency stop, checkpoint, recovery, replay, audit, invariants, AFIS, ABL, authority — ending `SYSTEM STATUS: RECONCILED`). Deterministic control only: no ML, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Treasury/Risk/Portfolio/Execution authority, no second AEGIS.

## Sprint 032 — Adaptive Execution Intelligence Engine

Added the deterministic, paper-only **Adaptive Execution Intelligence** layer on top of the Sprint 031 Execution Simulation engine. `services/market-engine/src/execution/intelligence/` closes the loop **Simulation Result → Telemetry → Signals → Quality → Adaptive Decision → Proposal → Validation → Apply → Record**, re-simulating revised plans until the work completes, exhausts the cycle budget, or fails closed. **ADAPTIVE INTELLIGENCE ≠ AUTHORITY. PAPER ONLY.** The controller only ever *proposes* REPRICE / RESLICE / REROUTE / REPLAN / ABORT actions (KEEP is the safe fallback); every proposal is `requiresExecutionAuthorization: true` with `treasuryMutation/riskMutation/portfolioMutation: false`; application goes exclusively through deterministic plan revisions with explicit lineage (plan-v1→v2→v3, immutable parents, never overwriting execution history); revised plans carry `riskRevalidation`/`aegisRevalidation` PENDING requirements. Emergency stop dominates every policy and aborts terminally; any invariant violation fails closed. It introduces: immutable `ExecutionTelemetry` (planned/submitted/filled/remaining, fill + completion ratio, quantity-weighted side-aware slippage in bps, fees, cost, latency, impact, per-order and per-venue rollups, rejection/cancellation/partial-fill/atomic-group state), 12 deterministic threshold evaluations (breached + severity), 13 canonical signal types (FILL_RATE_LOW, SLIPPAGE_HIGH, LATENCY_HIGH, LIQUIDITY_DETERIORATION, VENUE_DEGRADED, VENUE_FAILED, ORDER_AGING, PRICE_DRIFT, PARTIAL_FILL, ATOMIC_RISK, EXECUTION_COST_HIGH, EXECUTION_QUALITY_DEGRADED, EXECUTION_QUALITY_RECOVERING) each with evidence + fingerprint, a 7-dimension weighted Execution Quality assessment (FILL/PRICE/LATENCY/LIQUIDITY/COST/VENUE/COMPLETION with fixed A–F grades and trend detection), deterministic venue health (HEALTHY/DEGRADED/UNAVAILABLE/RECOVERING from latency/rejection/fill-quality/liquidity/staleness/simulation-failures with recovery hysteresis and routing multipliers), order-aging assessment, six adaptive policies with a strict dominance order ABORT > REPLAN > REROUTE > RESLICE > REPRICE > KEEP (all always evaluated, best applicable wins; ES always ABORT; atomic risk with no alternative ABORTs, with alternative REPLANs; reslice suppressed for atomic groups; reslice yields to reprice when the price is drifting), the deterministic Adaptive Execution Controller with its 9-stage lifecycle (OBSERVE→MEASURE→SCORE→SIGNAL→DECIDE→PROPOSE→VALIDATE→APPLY→RECORD), proposal engines for each action (tick-aligned band-clamped reprice with a hard max-reprice band — impossible reprices return null and escalate; reslice covering exactly the remainder with per-venue distribution and deterministic delays; reroute by 8-factor venue scoring with exclusion and a deterministic advantage threshold; replan preserving leg identity, route/leg liquidity caps and total quantity, returning null when candidates cannot cover the remainder; abort with full cancellation), fail-closed proposal validation (tick/band/quantity/coverage/authority checks + one applied action per plan+cycle dedupe), the `ExecutionIntelligenceEngine` closed loop (per-cycle AEGIS + Treasury authorization gates → BLOCKED; COMPLETED/ABORTED/BLOCKED/EXHAUSTED terminal states; venue-health hysteresis carried across cycles), hard invariants (immutable initial plan, lineage start/version/orphans, total quantity preservation, Treasury/Risk/Portfolio/Execution authority markers, AEGIS/Treasury authorization every cycle, ES dominance, dedupe, atomic integrity incl. ABL semantic sides, domain consistency, credential-field scan, decision fingerprint verification, replay equivalence), deterministic replay (`replayIntelligence` — identical inputs reproduce identical signals/quality/decisions/proposals/lineage/final state/fingerprint), and a hash-chained tamper-evident audit log (`oship.execution-intelligence.v1`, 12 event types, GENESIS-linked SHA-256 chain with `verify()`). A single shared intelligence layer serves AFIS and ABL. Covered by **364 new tests** (telemetry 23, quality 25, thresholds 22, signals 25, policies 28, decision 20, reprice 20, reslice 20, reroute 25, replan 25, venue-health 20, order-aging 12, controller 22, feedback 10, engine 16, replay 12, audit 15, invariants 24) and `pnpm demo:execution-intelligence` (KEEP, REPRICE, RESLICE, REROUTE, REPLAN, ABORT, VENUE DEGRADATION, VENUE FAILURE, PARTIAL FILL, HIGH LATENCY, HIGH SLIPPAGE, FAIL-CLOSED REPLAN, REPLAY, AUDIT, INVARIANTS — ending `SYSTEM STATUS: RECONCILED`). Adaptive only: no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Treasury/Risk/Portfolio/Execution authority, no second AEGIS, no ML — every decision is a deterministic, replayable policy evaluation with explicit reason + evidence.

## Sprint 031 — Deterministic Market Microstructure & Execution Simulation Engine

Added the real deterministic, paper-only Market Microstructure & Execution Simulation layer on top of the Sprint 030 Execution Plan engine. `services/market-engine/src/simulation/execution/` consumes an already-authorized Execution Plan and simulates its execution against a deterministic market model down to canonical fills, positions, metrics, quality, reconciliation, replay and an audit record. **SIMULATION ≠ EXECUTION AUTHORITY. SIMULATION ≠ LIVE TRADING. PAPER ONLY.** It introduces a canonical simulation model: deterministic `SimulationMarket` (market_id, venue_id, instrument_id, timestamp, sequence, bid/ask levels with price/quantity/sequence, last_price, spread, depth, trade_flow, status), `MarketEvent` (BOOK_SNAPSHOT / BOOK_UPDATE / TRADE / QUOTE / MARKET_STATUS / VENUE_STATUS / LATENCY with monotonic sequences + fingerprint), `VenueModel` (HEALTHY/DEGRADED/UNAVAILABLE, latency, fees, liquidity, capacity, order book), `Order` (order_id, plan_id, route_id, slice_id, venue_id, instrument_id, side, order_type, quantity, remaining_quantity, limit_price, status, time_in_force, created_at, sequence, fingerprint), `Fill`, `ExecutionSlice`, `AtomicGroupState`, `ExecutionMetrics`, `ExecutionQualityScore`, `ReconciliationResult`, `PositionResult` and `ExecutionSimAuditRecord`. All IDs and fingerprints are canonical SHA-256; a deterministic injected clock (no `Date.now` / `Math.random` / random UUID) drives all sequence/time. The versioned simulation configuration (simulation_config / matching_policy / fee_policy / latency_policy / slippage_policy / market_impact_policy + versions) participates in the simulation fingerprint so every replay reports exactly which policies produced a result. The deterministic PRICE_TIME matching engine (`consumeBook` / `executeOrderType`) matches better-price-first, earlier-sequence-first; MARKET consumes the book, LIMIT matches executable levels (buy ≤ limit, sell ≥ limit), IOC executes available then cancels the remainder (no residual live order), FOK is atomic all-or-nothing (no partial), POST_ONLY never crosses (REJECTED if marketable). Fees are maker/taker/fixed; latency is composable network+venue+matching+ack; slippage/market-impact is a replaceable deterministic policy (VWAP, realized slippage_bps, price_impact; no synthetic improvement). Slicing decomposes a plan into `ExecutionSlice`s (planned/submitted/filled/remaining/cancelled/rejected, status, sequence, fingerprint) with `defaultSubmittedFn` mapping an unavailable/halted venue to submitted 0 (remaining measured against submitted → quantity conservation holds). UNAVAILABLE venues accept no new orders (fail closed). Atomic groups (`isAtomicStrategy` = coordinated ∪ CROSS_VENUE_ARBITRAGE) evaluate to COMPLETE/PARTIAL/FAILED with a deterministic recovery (CANCEL_REMAINDER/HEDGE/REROUTE/REPRICE/REPLAN/ABORT); the engine executes the configured policy and never invents strategy. Fail-closed invariants (filled ≤ submitted, remaining ≥ 0, filled + remaining + cancelled (+ rejected) = submitted, FOK never partial, IOC never live remainder, POST_ONLY never TAKER-filled, fee ≥ 0, slippage ≥ 0, cancelled orders cannot fill, unknown venues cannot fill, position delta = net fills, atomic incomplete WITH recovery, reconciliation balances). An explainable deterministic Execution Quality score (fill_ratio, slippage, fees, latency, market_impact, completion_ratio — no ML). Position integrates through the existing Position subsystem (position delta = net fills; no per-venue position authority, no parallel position engine); reconciliation through the existing layer. Deterministic `chooseRecovery` maps a FailureClass to one recovery action always respecting Plan/Risk/AEGIS/Treasury boundaries; `compareExecutionReplay` proves identical orders/fills/VWAP/fees/latency/positions/reconciliation/fingerprint. `buildExecutionSimAudit` emits `oship.execution-sim.v1`. A single shared simulation infra serves AFIS and ABL (cross-venue/triangular/funding/basis/market-making/liquidity-imbalance and surebet/back-lay/+EV/hedge/middle) — no ABL-specific simulator. Covered by **272 new tests** (matching 41, order-lifecycle 25, order-types 25, partial-fill 20, fees-slippage 22, latency-impact 19, multi-venue 20, atomic 15, recovery 20, replay 15, reconciliation 15, invariants 20, scenarios 15), `pnpm demo:execution-simulation` (`SYSTEM STATUS: RECONCILED / REPLAY: PASS / INVARIANTS: PASS`, FULL FILL, PARTIAL FILL, SLIPPAGE+FEES, IOC, FOK, POST_ONLY, MULTI-VENUE, VENUE FAILURE, ATOMIC FAILURE, LATENCY, REPLAY, RECONCILIATION+AUDIT), and updated architecture/roadmap/glossary documentation. Simulation only — no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Execution/Risk/Portfolio/Treasury authority, no second AEGIS, no ML matching/routing/quality, no randomness in canonical math.

## Sprint 030 — Unified Execution Planning & Smart Routing Engine

Added the real deterministic Execution Planning & Smart Routing layer on top of the Sprint 029 Risk Decision engine. `services/market-engine/src/execution/planning/` answers **WHAT / WHERE / WHEN / HOW** to convert a Risk-approved Allocation Decision into a deterministic, auditable, replayable **Execution Plan** — and never executes. It introduces a canonical execution plan model (execution_plan_id, allocation_id, opportunity_id, strategy_id, domain, status, requested/approved/planned/unplanned capital, venue/route/order/leg counts, execution_mode, routing_policy, slicing_policy, estimated slippage/fees/latency, expected_fill_ratio, liquidity_utilization, time_horizon, deadline, freshness, risk/allocation/aegis/treasury reference, config/policy version, timestamp, fingerprint). The lifecycle is PROPOSED→VALIDATED→ROUTED→SLICED→READY→AEGIS_APPROVED→TREASURY_AUTHORIZED→PAPER_EXECUTED→RECONCILED plus terminal BLOCKED/STALE/EXPIRED/CANCELLED/FAILED/PARTIALLY_EXECUTED with explicit deterministic transitions. Six execution modes (SINGLE_VENUE/MULTI_VENUE/SEQUENTIAL/PARALLEL/HEDGE_FIRST/LEG_FIRST) are governed by strategy semantics. A deterministic Smart Router scores healthy routes by net economics → fill probability → liquidity → slippage → fees → latency → venue ID (stable tie-break, no ML/no randomness) and enforces multi-venue allocation (sum(routes) ≤ approved; route ≤ executable liquidity). Deterministic slicing (FIXED_SIZE, PERCENTAGE, LIQUIDITY_PROPORTIONAL, VWAP_STYLE, TWAP_STYLE), partial-fill handling (FULL/PARTIAL/UNFILLED; REMAIN_ON_VENUE/REROUTE/RESIZE/CANCEL/REPLAN), atomic & coordinated legs (triangular/funding/basis/hedge/surebet groups never silently split; unavailable mandatory leg → BLOCKED), freshness/expiry re-checks (stale/expired never reach AEGIS), AEGIS + Treasury boundaries (never self-authorize; Treasury proposal is recommendation-only), fail-closed invariants, deterministic replay, and a structured audit record (`oship.execution-plan.v1`). Covered by **188 new tests** (core 25, scenarios 22, routing 17, slicing 15, partial-fill 14, replay 10, invariants 16, atomic 15, cross-domain 10, replan 15, boundaries 18, economics 11), `pnpm demo:execution-planning` (`RECONCILED`, FULL/PARTIAL/BLOCKED/REROUTED/REPLAN, AFIS + ABL unified), and updated architecture/roadmap/glossary documentation. **Execution Planning ≠ Execution Authority** — simulation only; no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no second Portfolio/Risk/Execution authority, no ML, no routing randomness.

## Sprint 029 — Unified Portfolio Risk Decision & Risk Budget Engine

Added the real deterministic Risk Decision layer on top of the Sprint 028 Allocation engine. `services/market-engine/src/risk/decision/` answers **IS THE ALLOCATION SAFE** — the single Portfolio Risk Authority that decides whether a proposed Allocation is compatible with the current Portfolio, Risk Budget, Exposure, Correlation, and AFIS + ABL limits, or should be reduced / scaled / blocked, before passing to AEGIS/Treasury. It introduces a canonical risk decision model: projected total/domain/strategy/opportunity/position/event/correlation/instrument exposure, liquidity exposure, capital-at-risk, max loss, expected loss, risk-adjusted return, concentration, utilization, risk budget remaining, available capital, stress loss, worst-case portfolio impact, confidence, freshness, expiry, and a deterministic risk decision + reason + fingerprint. The lifecycle is PROPOSED→ASSESSED→RISK_CHECKED→RISK_APPROVED plus terminal REJECTED/RISK_BLOCKED/CAPITAL_BLOCKED/LIQUIDITY_BLOCKED/CORRELATION_BLOCKED/CONCENTRATION_BLOCKED/STALE/EXPIRED/CANCELLED. The non-binary scale is FULL_APPROVAL / PARTIAL_APPROVAL / REDUCED / BLOCKED; exposure/concentration/liquidity/drawdown/stress violations are *scalable* (they reduce approved capital to the risk-safe amount) while emergency-stop/stale/expired/all-or-nothing-below-minimum are fatal and block outright. AFIS and ABL are assessed in ONE unified risk budget with no preferential treatment. Includes versioned risk config (risk_config_version / risk_policy_version / risk_budget_version + configuration fingerprint), a unified hierarchical risk budget (total → domain → strategy → candidate), a deterministic composite risk score with observable factors (exposure, concentration, correlation, liquidity, drawdown, stress loss, capital-at-risk, freshness, confidence), a deterministic stress engine (NORMAL / ADVERSE / SEVERE / EXTREME with portfolio loss, candidate loss, domain loss, budget utilization, remaining budget), fail-closed invariant checks (allocated ≥ 0, approved ≤ requested, approved ≤ risk-safe, blocked candidate not approved, emergency stop not bypassed, all-or-nothing no sub-minimum, exposure ≤ limits, stress loss ≤ limit, budget utilization ≤ 100%), portfolio-aware projected portfolio (existing + new), dynamic reallocation-aware sequential budget accounting (a running portfolio snapshot enforces shared capital/exposure across the batch), Control integration (EMERGENCY_STOP/HALTED → BLOCKED, revalidation via CONTINUE/REVALIDATE/REJECT/EXPIRED/STALE), AEGIS boundary (APPROVED/PARTIALLY_APPROVED/BLOCKED — risk never bypasses AEGIS), Treasury boundary (proposal/recommendation only, never mutates treasury), canonical SHA-256 fingerprinting, isolated deterministic replay, and a structured audit record (`oship.risk.v1`). Covered by 120+ new tests (71 core + 49 cross-domain/stress/replay/invariant scenarios), `pnpm demo:risk-engine` (`RECONCILED`, FULL/PARTIAL/BLOCKED + all four stress scenarios), and updated architecture/roadmap/glossary documentation. Simulation only — no ML, no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass, no architecture duplication.

## Sprint 028 — Unified Capital Allocation & Portfolio Optimization Engine

Added the real deterministic allocation layer on top of the Sprint 026 opportunity engine + Sprint 027 strategy engine. `services/market-engine/src/allocation/optimizer/` answers HOW MUCH of the ONE unified OSHIP Treasury should be committed to each eligible (Opportunity + selected Strategy) candidate. It introduces a canonical allocation model (allocation_id, opportunity_id, strategy_id, domain, requested/allocated/unallocated capital, allocation_ratio, expected gross/net/risk-adjusted return, capital_efficiency, confidence, liquidity, correlation_group + correlation_exposure, portfolio_exposure, risk_score, time_horizon, capital_duration, capital_turnover, reason, policy_version, configuration_version, timestamp, fingerprint), a deterministic lifecycle (PROPOSED→EVALUATED→OPTIMIZED→RISK_APPROVED→AEGIS_APPROVED→TREASURY_AUTHORIZED→ALLOCATED plus terminal REJECTED/RISK_BLOCKED/CAPITAL_BLOCKED/AEGIS_BLOCKED/TREASURY_BLOCKED/EXPIRED/STALE/CANCELLED), a validated candidate builder (with re-checked opportunity expiry/freshness, required/maximum/minimum capital, gross/net/risk-adjusted return, edge, confidence, liquidity, execution probability, correlation-group, time horizon, all-or-nothing vs partial mode derived from the strategy type), a transparent composite allocation scoring policy (Fixed / Confidence Weighted / Edge Weighted / Capital Efficiency Weighted / Risk Adjusted / Liquidity Constrained / Correlation Adjusted / Hybrid — every factor observable), and explicit capital constraints (total/domain/strategy/position/event/correlation exposure, per-candidate cap, minimum viable allocation, liquidity reserve). Allocations NEVER mutate Treasury / Portfolio / Risk / AEGIS / Execution; Allocation is proposal-only.

Core deliverable is a deterministic, bounded greedy constrained-capital optimizer: filter invalid/stale → score → sort with stable tie-breakers (risk-adjusted return, capital efficiency, confidence, opportunity id, strategy id) → clamp partial candidates to the binding capital/exposure/liquidity budget → enforce all-or-nothing and minimum-viable-allocation → verify invariants (sum(allocations) ≤ allocatable, available+reserved+allocated = total, per-domain/correlation/position/event exposure ≤ limits, allocation ≤ executable liquidity) → fail closed to ALLOCATION_BLOCKED. It is cross-domain: AFIS and ABL compete for ONE Treasury in a single optimization (no separate pools), uses the existing Risk/Portfolio/correlation/liquidity model, supports dynamic reallocation with per-candidate delta (previous/new/delta), integrates Control (REVALIDATE/REPRICE/RESIZE and EMERGENCY_STOP/HALTED → NO_NEW_ALLOCATION while keeping reconciliation/audit/replay), AEGIS (must approve, cannot override), Treasury (proposal only, remains authoritative), and revalidation, plus canonical SHA-256 fingerprinting, isolated deterministic replay, and a structured audit record (optimization_id, candidate_ids, strategy_ids, input/reserved/available capital, scores, ranking, allocations, rejections, constraints, policy/config version, decision, reason). Covered by 115+ new tests (84 core + 24 cross-domain/AFIS/ABL/scenario), `pnpm demo:allocation-engine` (`RECONCILED`, shared capital, candidates, optimization, final allocation, unallocated capital, invariants PASS, Allocation→AEGIS→Treasury→paper execution→position→reconciliation), and updated architecture/roadmap/glossary documentation. Deterministic greedy only; no ML / quadratic / linear programming this sprint (the contract is replaceable by QP/LP/CVaR/mean-variance/RL later). Simulation only: no live exchange/bookmaker APIs, no provider credentials, no real-money movement, no authority-chain bypass.

## Sprint 027 — Unified Strategy Intelligence & Autonomous Strategy Selection

Added the real deterministic strategy layer on top of the canonical Sprint 026 unified multi-venue opportunity discovery engine. `services/market-engine/src/strategy/intelligence/` transforms a validated/ranked Opportunity into deterministic strategy candidates, evaluates each against a reusable unified economics model (expected return minus fees/slippage/latency/execution-failure/liquidity/capital/risk penalties), enforces explicit per-strategy limits, performs portfolio-aware concentration/capital checks, ranks admissible strategies by a configurable composite score (never raw return), and deterministically selects the best admissible strategy (or `NO_ADMISSIBLE_STRATEGY`). AFIS and ABL strategies compete for one shared capital budget in a single ranking. Added 25 strategy templates (AFIS cross-venue direct/conservative/latency-aware/capital-efficient, triangular, funding carry/basis+funding/hedged, basis convergence/market-neutral, market-making passive/inventory-aware/adaptive-spread, liquidity momentum/mean-reversion; ABL surebet equalized/risk-minimized/capital-efficient, back/lay balanced/profit-lock/min-liability, plus-EV flat/confidence-weighted/edge-weighted, hedge balanced/asymmetric/capital-minimized), strategy identity/fingerprinting/versioning, lifecycle state machine (PROPOSED→EVALUATED→RANKED→SELECTED→ALLOCATED→AUTHORIZED→EXECUTING→COMPLETED plus terminal/rejection states), a deterministic registry with health/enable/disable, control-plane advisory (CONTINUE/REVALIDATE/RESIZE/REROUTE/ABORT/HEDGE/PAUSE/HALT), structured audit records, and isolated deterministic replay. Covered by 91 new tests, `pnpm demo:strategy-engine` (`SYSTEM STATUS: RECONCILED`, replay PASS), and updated architecture/roadmap/glossary documentation. Strategy is proposal-only and NEVER mutates Portfolio / Risk / Allocation / AEGIS / Treasury / Execution; the execution venue remains deterministic simulation — no live exchange/bookmaker APIs, no provider credentials, no real-money movement, and no authority-chain bypass.

## Sprint 026 — Unified Multi-Venue Opportunity Discovery Engine

Added the canonical real opportunity layer. `services/market-engine/src/opportunity/` turns normalized market/odds intelligence into validated, ranked opportunities with a unified cost stack (gross edge minus fees/slippage/latency/adverse-selection/execution-failure/liquidity/capital/risk), a fail-closed liquidity evaluator, freshness + evidence-conflict detection (→ CONFLICTED/STALE), deterministic deduplication, a canonical lifecycle (CANDIDATE→VALIDATED→RANKED→ELIGIBLE→CONSUMED plus EXPIRED/REJECTED/STALE/CONFLICTED/INVALID/BLOCKED), multi-factor ranking, AFIS detectors (cross-venue, triangular, funding, basis, market-making, liquidity-imbalance), ABL detectors (2/3-way surebet, back/lay, value, hedge/middle), an isolated deterministic replay module, and `demo:opportunity-engine`. Opportunity discovery is proposal-only and never mutates Strategy / Risk / Allocation / AEGIS / Treasury / Execution. No live APIs, no provider credentials, no real-money movement, no authority-chain bypass.

## Sprint 025 — Adaptive Decision, Recovery & Autonomous Control Plane

Added the adaptive autonomous control plane on top of the deterministic pipeline. Built a real control engine with a kill-switch state machine (ACTIVE/DEGRADED/PAUSED/HALTED/EMERGENCY_STOP), a deterministic control decision engine, exactly-once idempotency (`control:{correlationId}:{decisionId}:{action}`) and an append-only hash-chained `oship.control.event.v1` journal + event chain. Added dynamic re-evaluation at sensitive pipeline boundaries, a bounded execution feedback loop, direct Sprint 024 recovery-to-control integration, deterministic adaptive strategy selection (AFIS + ABL), bounded risk-aware AEGIS-gated Treasury-authorized capital reallocation, and a full global vertical-slice isolated replay (LIVE STATE != REPLAY STATE; replay never mutates live state). EMERGENCY_STOP blocks new opportunities, allocations, execution and Treasury reservations while keeping reconciliation, audit, replay and settlement recovery available. Covered by 50+ new Sprint 025 tests, `pnpm demo:control` (`SYSTEM STATUS: RECONCILED`), and updated architecture/roadmap/glossary documentation. The execution venue remains deterministic simulation only; no live exchange/bookmaker APIs, no provider credentials, no real-money movement, and no authority-chain bypass.

## Sprint 024 — Reliability + Recovery Plane

Added the real, deterministic internal Reliability and Recovery plane. The reliability layer (failure detection/classification with a 23-class taxonomy, health monitoring, circuit breakers, timeout management, retry policy/engine, degradation modes, and a hash-chained reliability event log) feeds a recovery layer that classifies failures, selects an authority-respecting recovery action through a deterministic decision engine, executes it via the recovery orchestrator's 14-state machine and exactly-once attempt store, journals every transition in a hash-chained recovery log, reconciles against the Sprint 023 state plane, and replays into isolated clones to prove determinism. The recovery plane is strictly subordinate to AEGIS → Treasury authorization → Execution authorization → State Plane and can never mutate Treasury/Position/Portfolio directly. Everything uses an injected clock and canonical SHA-256 (no Math.random/Date.now). Added 50+ new reliability/recovery tests, a deterministic fault injector, `pnpm demo:recovery` (reports `SYSTEM STATUS: RECONCILED` and `Original == Replay: PASS`), and updated architecture/roadmap/glossary documentation. The execution venue remains deterministic simulation only; no live exchange/bookmaker APIs, no provider credentials, no real-money movement, and no authority-chain bypass.

## Sprint 023 — Unified Treasury + Event-Driven State Plane

Built the real, stateful OSHIP financial state plane. Added a unified domain-neutral Treasury engine (authorization → reservation → consumption → settlement) using integer minor-unit accounting with fail-closed capital invariants and a SHA-256 hash-chained ledger. Made the Position Engine and Portfolio stateful and event-driven, added exactly-once idempotency, full state-plane reconciliation, and isolated deterministic replay. Added executable AFIS/ABL end-to-end state-plane scenarios, 70+ new Sprint 023 tests, `pnpm demo:state-plane`, and updated architecture/roadmap/glossary documentation. The execution venue remains deterministic simulation only; no live exchange/bookmaker APIs, no provider credentials, no real-money movement, and no direct Treasury/execution bypass.

## Sprint 015

Added a deterministic unified agent runtime, AFIS and ABL discovery agents, opportunity scoring and deduplication, confidence evidence aggregation, strategy contracts/evaluation, audit records, heartbeat monitoring, and replay/demo infrastructure. Agents cannot directly access credentials, Treasury, or execution.


## Sprint 016 — OIIN Unified Event Intelligence Fabric

OIIN is the domain-neutral canonical event plane between external connectors and AFIS/ABL consumers. It validates and normalizes source payloads, assigns SHA-256 deterministic IDs, deduplicates, causally orders, correlates, persists, publishes through an isolated in-memory bus, and supports immutable replay envelopes. Connector credentials and raw payloads remain outside agents; OIIN never authorizes Treasury or execution. Invalid events are dead-lettered with stage and trace metadata.

## Sprint 017

Added the unified deterministic Market Intelligence Engine with features, signals, fair value, edge, evidence, confidence calibration, regimes, anomaly detection, eligibility, replay, and public APIs. Intelligence cannot authorize Treasury or execution.

## Sprint 018

Added unified portfolio, position, exposure, risk, allocation, deterministic integer-unit accounting, risk constraints, liquidity reserve, drawdown protection, and cross-domain capital competition foundations.

## Sprint 019

Added execution orchestration, smart routing, authorization validation, multi-leg execution plans, partial fills, immutable receipts, and reconciliation boundaries.

## Sprint 020

Added execution intelligence, adaptive smart routing, deterministic multi-venue simulation, execution/position event contracts, partial-fill behavior, and full reconciliation foundations.

## Sprint 021

Added the end-to-end orchestration spine, explicit state transitions, deterministic journal/hash chain, idempotency protection, and orchestration reconciliation/replay foundations.

## Sprint 022

Added the executable OIIN-to-execution market pipeline with real subsystem calls, end-to-end stage context, idempotency, position/settlement outputs, reconciliation, and AFIS/ABL deterministic scenarios.
