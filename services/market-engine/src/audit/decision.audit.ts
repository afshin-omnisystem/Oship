import {AuditTrail} from './agent.audit';export const auditDecision=(audit:AuditTrail,decision:any,timestamp:string)=>audit.record({traceId:decision.correlationId,correlationId:decision.correlationId,causationId:decision.opportunityId,timestamp,actor:decision.strategyId,component:'StrategyEngine',action:'strategy.evaluation',result:decision.action,reason:decision.reason,version:'1.0'})
