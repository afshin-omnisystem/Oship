export interface RouteCandidate{routeId:string;venue:string;price:number;liquidity:number;fees:number;slippage:number;latencyMs:number;reliability:number;fillProbability:number;netEdge:number;healthy:boolean}export interface RouteScore{value:number;policyVersion:string;components:Record<string,number>}export interface RouteDecision{routeId:string;selected:boolean;score:RouteScore;reason:string}export interface RoutingPolicy{version:string;maxLatencyMs:number;maxSlippage:number;minFillProbability:number;weights:Record<string,number>}
