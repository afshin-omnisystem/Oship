import {OiinEvent} from '../oiin';import {IntelligenceRecord} from '../intelligence';import {Opportunity} from '../discovery';import {StrategyDecision} from '../strategy';import {PortfolioSnapshot} from '../portfolio';import {RiskEvaluation} from '../risk';import {AllocationProposal} from '../allocation';import {AegisEvaluation} from '../core/aegis-boundary';import {ExecutionIntelligenceRecord} from '../execution-intelligence';import {AdaptiveRouteDecision} from '../adaptive-routing';import {ExecutionPlan,ExecutionResult} from '../execution';import {PositionEvent} from '../position-events';import {Reconciliation} from './orchestration-reconciler';export interface PipelineContext{correlationId:string;traceId:string;event:OiinEvent;intelligence?:IntelligenceRecord;opportunity?:Opportunity;strategy?:StrategyDecision;portfolio?:PortfolioSnapshot;risk?:RiskEvaluation;allocation?:AllocationProposal;aegis?:AegisEvaluation;treasury?:{authorizationId:string;amount:number;state:'AUTHORIZED'|'RESERVED'|'CONSUMED'|'SETTLED'|'RELEASED'};executionIntelligence?:ExecutionIntelligenceRecord;route?:AdaptiveRouteDecision;plan?:ExecutionPlan;execution?:ExecutionResult;positionEvents?:PositionEvent[];reconciliation?:Reconciliation}
