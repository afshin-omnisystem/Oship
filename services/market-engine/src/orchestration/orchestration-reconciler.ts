import {OrchestrationContext,JournalEvent} from './types';export type OrchestrationReconciliationStatus='FULLY_RECONCILED'|'PARTIALLY_RECONCILED'|'RECONCILIATION_FAILED';export interface Reconciliation{reconciliationId:string;status:OrchestrationReconciliationStatus;reasons:string[]}import {sha256} from '../oiin/ids';export class OrchestrationReconciler{verify(c:OrchestrationContext,events:JournalEvent[]):Reconciliation{const reasons:string[]=[];if(!c.correlationId||!c.traceId)reasons.push('MISSING_CORRELATION');if(!events.length)reasons.push('EMPTY_JOURNAL');for(let i=1;i<events.length;i++)if(events[i].sequence!==events[i-1].sequence+1)reasons.push('SEQUENCE_MISMATCH');return{reconciliationId:`orch_rec_${sha256({c,events}).slice(0,16)}`,status:reasons.length?'RECONCILIATION_FAILED':'FULLY_RECONCILED',reasons}}}
