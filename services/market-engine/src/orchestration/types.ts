export type OrchestrationState='RECEIVED'|'INTELLIGENCE_READY'|'OPPORTUNITY_READY'|'STRATEGY_EVALUATED'|'RISK_EVALUATED'|'ALLOCATION_EVALUATED'|'AEGIS_EVALUATED'|'TREASURY_AUTHORIZED'|'EXECUTION_INTELLIGENCE_READY'|'ROUTE_SELECTED'|'EXECUTION_STARTED'|'EXECUTION_PARTIALLY_FILLED'|'EXECUTION_FILLED'|'POSITION_RECORDED'|'RECONCILING'|'RECONCILED'|'COMPLETED'|'REJECTED'|'EXPIRED'|'STALE'|'AUTHORIZATION_FAILED'|'RISK_BLOCKED'|'TREASURY_BLOCKED'|'EXECUTION_FAILED'|'RECONCILIATION_FAILED'|'REPLAY_FAILED';export interface OrchestrationContext{correlationId:string;traceId:string;parentEventId?:string;opportunityId:string;intelligenceId:string;strategyDecisionId:string;portfolioId:string;riskEvaluationId:string;allocationId:string;aegisEvaluationId:string;treasuryAuthorizationId:string;executionPlanId?:string;executionId?:string;positionId?:string}export interface Transition{previousState:string;nextState:string;timestamp:number;correlationId:string;traceId:string;reason:string;eventId:string}export interface JournalEvent extends Transition{sequence:number;parentEventId?:string;payloadHash:string;previousHash:string;chainHash:string}export interface OrchestrationResult{status:OrchestrationState;context:OrchestrationContext;transitions:JournalEvent[];reason?:string}
