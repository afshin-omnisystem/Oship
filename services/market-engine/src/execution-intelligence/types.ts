export interface ExecutionIntelligenceInput{opportunityId:string;intelligenceId:string;strategyDecisionId:string;executionPlanId:string;correlationId:string;timestamp:number;expectedPrice:number;expectedEdge:number;expectedFees:number;expectedSlippage:number;expectedLatency:number;expectedFillProbability:number;expectedAdverseSelection:number;venueReliability:number}
export interface ExecutionIntelligenceRecord{executionIntelligenceId:string;opportunityId:string;intelligenceId:string;strategyDecisionId:string;executionPlanId:string;correlationId:string;timestamp:number;expectedPrice:number;expectedEdge:number;expectedFees:number;expectedSlippage:number;expectedLatency:number;expectedFillProbability:number;expectedAdverseSelection:number;expectedExecutionCost:number;expectedNetValue:number;executionConfidence:number;routeConfidence:number;eligible:boolean;rejectionReasons:string[];policyVersion:string;deterministicHash:string}
