import {Opportunity,OpportunityScore,ScoringConfig} from './discovery.types'; export const DEFAULT_SCORING:ScoringConfig={version:'1.0',weights:{expectedEdge:.3,confidence:.2,liquidity:.15,executionQuality:.15,dataFreshness:.1,capitalEfficiency:.1,slippage:-.1,fees:-.1,latency:-.05,correlationRisk:-.1}};export function scoreOpportunity(o:Opportunity,c=DEFAULT_SCORING):OpportunityScore{const d:Record<string,number>={expectedEdge:o.expectedEdge,confidence:o.confidence,...o.conditions};const value=Object.entries(c.weights).reduce((s,[k,w])=>s+w*(d[k]??0),0);return {version:c.version,value:Number(value.toFixed(12)),dimensions:d}};
