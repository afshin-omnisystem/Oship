export type RiskDecision='APPROVED'|'APPROVED_WITH_RESTRICTION'|'REJECTED';export type RiskCode='MAX_TOTAL_EXPOSURE'|'MAX_DOMAIN_EXPOSURE'|'MAX_POSITION_SIZE'|'MAX_EVENT_EXPOSURE'|'MAX_CORRELATION_EXPOSURE'|'LIQUIDITY_RESERVE'|'DAILY_LOSS_LIMIT'|'DRAWDOWN_LIMIT'|'ANOMALY_BLOCK'|'REGIME_RESTRICTION';export interface RiskPolicy{version:string;maxTotalExposure:number;maxDomainExposure:Record<string,number>;maxSinglePosition:number;maxSingleEventExposure:number;maxCorrelationGroupExposure:number;minimumLiquidityReserve:number;maxDailyLoss:number;maxDrawdown:number}export interface RiskViolation{code:RiskCode;amount:number;limit:number;reason:string}export interface RiskEvaluation{riskEvaluationId:string;decision:RiskDecision;violations:RiskViolation[];policyVersion:string;timestamp:number;correlationId:string}
