import {AllocationMethod} from './types';export function size(method:AllocationMethod,requested:number,available:number,confidence:number,edge:number,cap:number){if(!Number.isFinite(requested)||requested<=0||cap<=0)throw new Error('invalid sizing');const factor=method==='CONFIDENCE_WEIGHTED'?confidence:method==='EDGE_WEIGHTED'?Math.max(0,edge):1;return Math.min(cap,available,Math.floor(requested*factor))}
