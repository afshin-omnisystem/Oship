import {createIdentity} from '../../agent/agent.identity';import {DiscoveryAgent,CandidateOpportunity} from '../../discovery/discovery.types';import {Observation,Permission} from '../../agent/agent.types';export abstract class AblDiscoveryAgent implements DiscoveryAgent{identity;constructor(role:string){this.identity=createIdentity({domain:'ABL',type:'DISCOVERY',role,version:'1.0.0',createdAt:'2025-01-01T00:00:00.000Z',permissions:['DISCOVERY_READ_ODDS','DISCOVERY_CREATE_OPPORTUNITY'] as Permission[]})}abstract discover(o:Observation):CandidateOpportunity[]}
