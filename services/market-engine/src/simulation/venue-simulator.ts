export interface SimulatedVenue{venue:string;bid:number;ask:number;depth:number;fees:number;latencyMs:number;healthy:boolean;reliability:number;fillProbability:number}export interface SimulatedFill{requested:number;filled:number;remaining:number;averagePrice:number;fees:number;slippage:number;status:'NONE'|'PARTIAL'|'FILLED'|'FAILED'|'CANCELLED'}export class VenueSimulator{constructor(readonly venues:ReadonlyArray<SimulatedVenue>){}fill(v:SimulatedVenue,quantity:number,ratio:number):SimulatedFill{if(quantity<0||ratio<0||ratio>1||quantity>v.depth)throw new Error('invalid simulation fill');const filled=quantity*ratio;return{requested:quantity,filled,remaining:quantity-filled,averagePrice:v.ask,fees:0,slippage:0,status:filled===0?'FAILED':filled===quantity?'FILLED':'PARTIAL'}}}
