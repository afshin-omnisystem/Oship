import {AfisDiscoveryAgent} from './afis.discovery.agent';import {Observation} from '../../agent/agent.types';import {CandidateOpportunity} from '../../discovery/discovery.types';export class CryptoArbitrageAgent extends AfisDiscoveryAgent{constructor(){super('CRYPTO_ARBITRAGE')}discover(o:Observation):CandidateOpportunity[]{const p=o.payload as any,a=p.venueA,b=p.venueB;if(!a||!b||a.ask==null||b.bid==null||b.bid<=a.ask)return [];const edge=b.bid-a.ask-(p.fees??0)-(p.slippage??0)-(p.transferCost??0)-(p.latencyPenalty??0);return [{domain:'AFIS',type:'CRYPTO_ARBITRAGE',instruments:[p.symbol],venues:[a.name,b.name],direction:'BUY_SELL',timeWindow:p.timeWindow??'instant',conditions:{liquidity:p.liquidity??1,executionQuality:p.executionQuality??1,dataFreshness:p.dataFreshness??1,fees:p.fees??0,slippage:p.slippage??0,latency:p.latencyPenalty??0,capitalEfficiency:p.capitalEfficiency??1,correlationRisk:p.correlationRisk??0},expectedEdge:edge,confidence:p.confidence??.8,discoveredAt:o.timestamp,agentId:this.identity.id,evidence:{bid:b.bid,ask:a.ask}}]}}
