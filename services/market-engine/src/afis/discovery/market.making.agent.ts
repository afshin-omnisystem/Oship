import {AfisDiscoveryAgent} from './afis.discovery.agent';import {Observation} from '../../agent/agent.types';import {CandidateOpportunity} from '../../discovery/discovery.types';export class MarketMakingAgent extends AfisDiscoveryAgent{constructor(){super('MARKET_MAKING')}discover(o:Observation):CandidateOpportunity[]{const p=o.payload as any;if((p.spread??0)<=0|| (p.depth??0)<=0)return [];return [{domain:'AFIS',type:'MARKET_MAKING',instruments:[p.symbol],venues:[p.venue],direction:'PROVIDE_LIQUIDITY',timeWindow:p.timeWindow??'short',conditions:{liquidity:Math.min(1,p.depth/100),executionQuality:1,dataFreshness:1,slippage:p.volatility??0,fees:p.fees??0,capitalEfficiency:p.inventory??1,correlationRisk:0},expectedEdge:p.spread-(p.fees??0),confidence:p.confidence??.7,discoveredAt:o.timestamp,agentId:this.identity.id}]}}
