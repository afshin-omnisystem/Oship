import {Domain} from '../agent/agent.types';import {Opportunity} from '../discovery/discovery.types';export type StrategyAction='REJECT'|'WATCH'|'PROPOSE'|'EXECUTE_REQUEST';export interface StrategyDecision{decisionId:string;strategyId:string;opportunityId:string;action:StrategyAction;confidence:number;expectedValue:number;capitalRequirement:number;riskParameters:Record<string,number>;reason:string;evidence:Record<string,unknown>;timestamp:string;correlationId:string}export interface StrategyContext{timestamp:string;correlationId:string}export interface Strategy{ id:string;domain:Domain;version:string;supportedOpportunityTypes:string[];requiredPermissions:string[];evaluate(o:Opportunity,c:StrategyContext):StrategyDecision}
