export type PositionStatus='OPEN'|'PARTIALLY_CLOSED'|'CLOSED'|'SETTLED'|'CANCELLED';export type PositionSide='LONG'|'SHORT'|'STAKE'|'LAY';export interface Position{positionId:string;domain:'AFIS'|'ABL';instrument:string;side:PositionSide;quantity:number;entryPrice:number;exposure:number;status:PositionStatus;correlationId:string;strategyId?:string;venue?:string;event?:string;timestamp:number}export interface PositionUpdate{positionId:string;quantity:number;price:number;status?:PositionStatus;timestamp:number;correlationId:string}export interface PositionLedgerEntry{entryId:string;positionId:string;kind:'OPEN'|'CLOSE'|'SETTLE';quantity:number;price:number;timestamp:number}
