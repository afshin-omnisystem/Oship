import {IntelligenceRecord} from './types';export class IntelligenceRegistry{private m=new Map<string,IntelligenceRecord>();register(i:IntelligenceRecord){if(this.m.has(i.intelligenceId))throw new Error('duplicate intelligence');this.m.set(i.intelligenceId,Object.freeze(i))}get(id:string){return this.m.get(id)}list(){return[...this.m.values()]}remove(id:string){this.m.delete(id)}}
