import {intelligenceHash} from './canonical';export function featureId(name:string,eventId:string){return`feat_${name}_${intelligenceHash({name,eventId}).slice(0,16)}`}export function signalId(type:string,features:string[]){return`sig_${type}_${intelligenceHash({type,features}).slice(0,16)}`}export function intelligenceId(domain:string,timestamp:string,body:unknown){return`intel_${domain}_${timestamp}_${intelligenceHash(body).slice(0,16)}`}export function evidenceId(sourceId:string,timestamp:string){return`evidence_${intelligenceHash({sourceId,timestamp}).slice(0,16)}`}export function anomalyId(type:string,sources:string[]){return`anomaly_${type}_${intelligenceHash({type,sources}).slice(0,16)}`}
