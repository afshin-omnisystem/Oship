export interface ExecutionPolicy{version:string;maxRetries:number;maxRouteLatency:number;minExpectedEdge:number;maxSlippage:number;minFillProbability:number;mode:'ATOMIC'|'SEQUENTIAL'|'BEST_EFFORT'|'HEDGE_REQUIRED'}export const DEFAULT_EXECUTION_POLICY:ExecutionPolicy={version:'execution.v1',maxRetries:0,maxRouteLatency:1000,minExpectedEdge:0,maxSlippage:.02,minFillProbability:.5,mode:'ATOMIC'};
