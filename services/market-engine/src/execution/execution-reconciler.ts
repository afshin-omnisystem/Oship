import {ExecutionResult} from './types';export class ExecutionReconciler{reconcile(r:ExecutionResult){if(!r.receipts.length)throw new Error('missing execution receipt');if(r.receipts.some(x=>x.filledQuantity>x.requestedQuantity))throw new Error('execution amount mismatch');return{reconciled:r.state==='SETTLEMENT_PENDING'||r.state==='SETTLED',requestId:r.requestId,receiptCount:r.receipts.length}}}
