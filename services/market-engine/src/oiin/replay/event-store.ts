import {OiinEventEnvelope} from '../types';export class InMemoryEventStore{private xs:OiinEventEnvelope[]=[];append(e:OiinEventEnvelope){this.xs.push(Object.freeze(e));return e}getById(id:string){return this.xs.find(x=>x.event.id===id)}byCorrelation(id:string){return this.xs.filter(x=>x.event.correlationId===id)}bySequence(n:number){return this.xs.find(x=>x.ingestionSequence===n)}range(from:number,to:number){return this.xs.filter(x=>x.event.timestamp>=from&&x.event.timestamp<=to)}all(){return[...this.xs]}}
