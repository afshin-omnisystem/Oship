import {OiinNormalizer,NormalizationContext} from './normalizer';import {createEvent} from '../event';export interface MarketInput{symbol:string;venue:string;bid:number;ask:number;last:number;volume:number;quoteCurrency?:string;baseCurrency?:string;eventTime:number}export class MarketNormalizer implements OiinNormalizer<MarketInput>{supports(x:unknown):x is MarketInput{return typeof x==='object'&&x!==null&&'symbol'in x&&'bid'in x}normalize(x:MarketInput,c:NormalizationContext){if(!this.supports(x)||!x.symbol||!Number.isFinite(x.bid)||!Number.isFinite(x.ask)||!Number.isFinite(x.last)||!Number.isFinite(x.volume)||x.bid<0||x.ask<x.bid||x.last<0||x.volume<0)throw new Error('invalid market payload');return createEvent({eventType:'MARKET_TICK',timestamp:x.eventTime,sourceTimestamp:x.eventTime,source:c.source,correlationId:c.correlationId,payload:{...x}})}}
