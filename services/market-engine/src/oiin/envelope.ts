import {OiinEventEnvelope} from './types';export function validateEnvelope(e:OiinEventEnvelope){if(!e.event||e.event.schemaVersion!=='oiin.event.v1'||!e.traceId||!Number.isFinite(e.receivedAt)||!Number.isInteger(e.ingestionSequence))throw new Error('invalid OIIN envelope');return e}export function envelope(e:OiinEventEnvelope):Readonly<OiinEventEnvelope>{validateEnvelope(e);return Object.freeze(e)}
