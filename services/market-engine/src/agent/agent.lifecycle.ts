import {AgentIdentity,AgentStatus} from './agent.types';
const transitions:Record<AgentStatus,AgentStatus[]>={CREATED:['INITIALIZING'],INITIALIZING:['ACTIVE','FAILED'],ACTIVE:['PAUSED','DEGRADED','STOPPING'],PAUSED:['ACTIVE','STOPPING'],DEGRADED:['ACTIVE','PAUSED','STOPPING','FAILED'],STOPPING:['STOPPED','FAILED'],STOPPED:[],FAILED:[]};
export function transition(agent:AgentIdentity,next:AgentStatus):AgentIdentity {if(!transitions[agent.status].includes(next)) throw new Error(`invalid transition ${agent.status} -> ${next}`); return {...agent,status:next,updatedAt:agent.updatedAt}}
export class AgentLifecycle {constructor(public agent:AgentIdentity){} start(){this.agent=transition(this.agent,'INITIALIZING');this.agent=transition(this.agent,'ACTIVE');return this.agent} pause(){this.agent=transition(this.agent,'PAUSED');return this.agent} resume(){this.agent=transition(this.agent,'ACTIVE');return this.agent} stop(){this.agent=transition(this.agent,'STOPPING');this.agent=transition(this.agent,'STOPPED');return this.agent} fail(){this.agent=transition(this.agent,'FAILED');return this.agent} healthCheck(){return this.agent.status==='ACTIVE'?'HEALTHY':this.agent.status==='DEGRADED'?'DEGRADED':'UNHEALTHY'}}
