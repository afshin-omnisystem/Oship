import {AgentIdentity,AgentId} from './agent.types'; export class AgentRegistry {private agents=new Map<AgentId,AgentIdentity>(); register(a:AgentIdentity){if(this.agents.has(a.id))throw new Error('agent already registered');this.agents.set(a.id,a);return a} get(id:AgentId){const a=this.agents.get(id);if(!a)throw new Error('unknown agent');return a} all(){return [...this.agents.values()]}}
